#
npm already is installed with node.

#
npm is used for server side node packages (e.g. lodash, gulp, grunt, yeoman,express, crypto, socket.io), but lately it is also becoming more popular for client side packages too (angular, bootstrap, knockout).Bower is the more popular client side package manager.
you use npm to install server packages and bower to install client packages.	node_modules,bower_components


#
JavaScript build automation tools		
npm install -g gulp grunt-cli

#
npm init --create package.json file
npm install
npm install bower
bower init
npm install grunt
npm install --save-dev grunt
